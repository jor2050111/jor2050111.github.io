# BPC170 Chapter 9: Supporting Mobile Devices

Open `index.html` in a modern browser or use a local web server.

Replace the starter slides with the chapter content, then run:

```bash
./scripts/validate-decks
```

Add this chapter to `slides/bpc170/index.html` before publication.

The deck starts with `data-palette="phoenix-classic"`. See
`slides/shared/visual-style.md` before choosing another brand palette.
