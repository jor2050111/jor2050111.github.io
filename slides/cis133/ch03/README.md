# CIS133 Chapter 3: Semantic HTML and Images

Open `index.html` in a modern browser or use a local web server.

The completed deck uses a field-guide notebook treatment and the
`canyon-sand` palette. Run the validator from the repository root:

```bash
./scripts/validate-decks
```

Generated teaching visuals live in `assets/` beside the deck.
