# CIS133 Chapter 2: Basic HTML Elements

Open `index.html` in a modern browser or use a local web server.

The completed deck uses a blueprint workbench treatment and the
`golden-hour` palette. Run the validator from the repository root:

```bash
./scripts/validate-decks
```

Generated teaching visuals live in `assets/` beside the deck.
