# CIS215 Chapter 7: Data Visualization with matplotlib

Open `index.html` through the repository's local web server.

The deck uses CSS and inline SVG only. Run `./scripts/validate-decks`
after changes.
